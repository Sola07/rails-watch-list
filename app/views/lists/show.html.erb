<div class="container">

<h1 class="text-center"><%= @list.name %></h1>

<div class="card-group">
  <% @list.bookmarks.each do |bookmark| %>
  <div class="card m-3">
    <%= image_tag bookmark.movie.poster_url, width: 200, class: "card-img-top"%>
    <div class="card-body">
      <%= link_to bookmark_path(bookmark), data: {turbo_method: :delete, turbo_confirm: "Are you sure?" } do %>
      <i class="fa-solid fa-trash text-secondary small"></i>
      <% end %>
      <h5 class="card-title mt-2"><%= bookmark.movie.title %></h5>
      <p class="card-text"><%= bookmark.comment %></p>
    </div>
  </div>
  <% end %>
 </div>


<div class="mx-3">
<h4 class="mt-5">New bookmark</h4>
<%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.submit "Submit comment", class: "btn btn-primary" %>
<% end %>
</div>



<p class="mt-4"><%= link_to "Back to the list of lists", lists_path, class: "text-decoration-none text-dark" %></li></p>
</div>
